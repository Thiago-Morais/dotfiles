# [ BINDING OPTIONS ]
on-button-middle=dismiss-group
on-button-right=dismiss
on-touch=invoke-default-action
on-notify=exec mpv /usr/share/sounds/freedesktop/stereo/message.oga

# [ STYLE OPTIONS ]
font=JetBrainsMono NFP 11
height=200
width=350
margin=0
padding=10
border-size=2
border-radius=5
progress-color=source #0b1c1c
max-icon-size=85
icon-border-radius=10

default-timeout=10000
max-visible=10
# text-alignment=center
# ignore-timeout=1
# group-by=app-name
# Uncomment this line to show on fullscreen
# layer=overlay

include=~/.config/mako/mako-colors

# [ MODES OPTIONS ]
# Enable with  `makoctl mode -a do-not-disturb`
# Disable with `makoctl mode -r do-not-disturb`
# Toggle with  `makoctl mode -t do-not-disturb`

[mode=do-not-disturb]
invisible=1
on-notify=none

[mode=silent]
on-notify=none

[urgency=low]
on-notify=none

